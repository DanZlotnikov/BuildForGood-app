export const showBottomMenu = () => (
    {
       type: 'SHOW_BOTTOM_MENU', 
       payload : null
    }
)

export const getProfile = (profile) => (
    {
       type: 'GET_PROFILE', 
       payload : profile
    }
)